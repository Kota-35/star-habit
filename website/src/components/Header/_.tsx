import { Star, User } from "lucide-react";
import { Button } from "../ui/button";

export const Header = () => {
  return (
    <header className="sticky top-0 z-50 shrink-0 border-gray-200 border-b bg-white shadow-sm">
      <div className="flex h-10 items-center justify-end px-6"></div>
    </header>
  );
};
