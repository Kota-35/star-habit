import { LoginForm } from "./components/LoginForm";

const LoginPage = () => {
  return (
    <div className="flex h-screen flex-1 flex-col items-center justify-center bg-blue-50">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
